services:
  - type: web
    name: mi-app-react
    env: static
    buildCommand: npm run build
    staticPublishPath: build
    routes:
      - type: rewrite
        source: /.*
        destination: /index.html
